As a reminder you are the developer with the goal to maximize the total number of points in this experiment, without regard for the other players in the experiment. You will now be taking part in the **first declaration phase**.

*total number of points* =  (The Real Value of the property under the chosen condition) – ((the first declaration * {{ public_information.initial_tax_rate }}% tax) + (final declaration * {{ public_information.final_tax_rate }}% tax)) + (first speculation result + final speculation result) + ((cash position) +(number of shares * true share value in the chosen condition))

You will receive the following game information of the state of the game:

1. **Game Information**:
   - Phase: {{ meta.phase_name }} (Phase {{ meta.phase }})
   - Your Role: Developer (Player #{{ meta.player_number }})
   - Name: {{ meta.player_name }}

2. **Available Information**:
  2.1. **Your True Land Values**:
    - {{ private_information.property.name }}:
      - No Project: {{ private_information.property.v[0] }}
      - Project: {{ private_information.property.v[1] }}

  2.2. **Value Boundaries**:
    {% for role, conditions in public_information.boundaries.items() %}
    - {{ role }}:
      {% for condition, bounds in conditions.items() %}
      - {{ condition }}: {{ bounds.low }} to {{ bounds.high }}
      {% endfor %}
    {% endfor %}

  2.3. **Tax Information**:
    - Current tax rate: {{ public_information.tax_rate }}%

The tax levied for the developer and the owners for the condition that is chosen in the experiment, is equal to {{ public_information.tax_rate }}%.

The instructions for the first declaration phase follow:

1. High-Level Reasoning for the First Declaration

1. Avoid Speculation Losses
   - If you declare a value lower than your true value, speculators can profit from buying your land (you lose half the difference).
   - If you declare a value higher than or equal to your real value, speculators would lose if they tried to buy, so they typically won’t buy at all.

2. Encourage the Project to Win
   - Market selects Project or No Project based on the median of recent trades.
   - Public signals (influenced by initial declarations) sway share price expectations.
   - Declare near the upper bound ({{ public_information.boundaries["developer"]["projectA"]["high"] }}) to influence the Project's perceived value upward.

3. Net Effect of Declaring Higher Values
   - Higher declarations increase {{ public_information.initial_tax_rate }}% tax, but you recoup some cost via your 30 tax shares.
   - High Project declaration discourages speculators and favors the Project being chosen.
   - No Project declaration should be just above your real value to avoid speculation losses.

2. Concrete “First Declaration” 

Given:
- Your True Land Values:
  - No Project: {{ private_information.property.v[0] }}
  - Project: {{ private_information.property.v[1] }}

- Known Boundaries (Developer):
  - No Project: {{ public_information.boundaries["developer"]["noProject"]["low"] }} to {{ public_information.boundaries["developer"]["noProject"]["high"] }}
  - Project: {{ public_information.boundaries["developer"]["projectA"]["low"] }} to {{ public_information.boundaries["developer"]["projectA"]["high"] }}

Strategy:
1. No Project Declaration
   - Declare min({{ private_information.property.v[0] + 1 }}, {{ public_information.boundaries["developer"]["noProject"]["high"] }})

2. Project Declaration
   - Declare min({{ private_information.property.v[1] + 1 }}, {{ public_information.boundaries["developer"]["projectA"]["high"] }})

3. **Your Decision to Make**:
   - Declare your land value for both scenarios (Project and No Project)
   - Your declaration will affect which scenario is chosen
   - Provide the output as a JSON object:

    {
        "gameId": {{ meta.game_id }},
        "type": "declare",
        "declaration": [
            <value_no_project>,
            <value_project>,
            0,
        ],
    }

This ensures we avoid speculation losses and strongly encourage the Project to be chosen.
