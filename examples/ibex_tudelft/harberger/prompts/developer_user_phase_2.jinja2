Instructions for this phase:

1. **Game Information**:
   - Phase: {{ meta.phase_name }} (Phase {{ meta.phase }})
   - Your Role: Developer (Player #{{ meta.player_number }})
   - Name: {{ meta.player_name }}

2. **Available Information**:
  2.1. **Your True Land Values**:
    - {{ private_information.property.name }}:
      - No Project: {{ private_information.property.v[0] }}
      - Project: {{ private_information.property.v[1] }}

  2.2. **Value Boundaries**:
    {% for role, conditions in public_information.boundaries.items() %}
    - {{ role }}:
      {% for condition, bounds in conditions.items() %}
      - {{ condition }}: {{ bounds.low }} to {{ bounds.high }}
      {% endfor %}
    {% endfor %}

  2.3. **Tax Information**:
    - Current tax rate: {{ public_information.tax_rate }}%

3. **Declaration Action Details**

- No Project Declaration:
  - We want this value to be as low as possible to lower the No Project sum.
  - Developer No Project bounds: {{ public_information.boundaries["developer"]["noProject"]["low"] }} to {{ public_information.boundaries["developer"]["noProject"]["high"] }}
  - Declare: <value_no_project> = {{ public_information.boundaries["developer"]["noProject"]["low"] }} 

- Project Declaration:
  - We want this value to be as high as possible to increase the Project sum.
  - Developer Project bounds: {{ public_information.boundaries["developer"]["projectA"]["low"] }} to {{ public_information.boundaries["developer"]["projectA"]["high"] }}
  - Declare: <value_project> = {{ public_information.boundaries["developer"]["projectA"]["high"] }} 

4. **Your Decision to Make**:
   - Declare your land value for both scenarios (Project and No Project)
   - Your declaration will affect which scenario is chosen
   - Provide the output as a JSON object:

    {
        "gameId": {{ meta.game_id }},
        "type": "declare",
        "declaration": [
            <value_no_project>,
            <value_project>,
            0,
        ],
    }
