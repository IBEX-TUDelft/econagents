This is your information about the state of the game and instructions for this phase:

1. **Game Information**:
   - Phase: {{ meta.phase_name }} (Phase {{ meta.phase }})
   - Your Role: Owner (Player #{{ meta.player_number }})
   - Name: {{ meta.player_name }}

2. ** Available Information**:
  2.1. **Your True Land Values**:
    - {{ private_information.property.name }}:
      - No Project: {{ private_information.property.v[0] }}
      - Project: {{ private_information.property.v[1] }}
  2.2. **Winning Condition**:
    - {{ public_information.winning_condition_description }} ({{ public_information.winning_condition }})
  2.3. **Your Initial Declaration**:
    - Declared Value: {{ private_information.declarations[meta.player_number-1].declared_value }}
    - Percentile: {{ private_information.declarations[meta.player_number-1].percentile }}%
  2.4. **Tax Information**:
      - Current tax rate: {{ public_information.tax_rate }}%

3. **Decision Logic**

1. **Identify the Winning Condition**
   - Let \(W\) be the chosen development option (0 = “No Project,” 1 = “Project”).

2. **Real Value**
   - Let \(R = ext{RealValue}_{W}\) be our true real value for the winning condition.

3. **Declare Exactly Our Real Value**
   - If \(W = 0\) (“No Project” won), set:
     - `value_no_project = {{ private_information.property.v[0] }}`
     - `value_project = 0` (placeholder)
   - If \(W = 1\) (“Project” won), set:
     - `value_project = {{ private_information.property.v[1] }}`
     - `value_no_project = 0` (placeholder)

4. **Your Decision to Make**:
   - Make your final declaration for the winning scenario
   - Provide the output as a JSON object:
    {
      "gameId": {{ meta.game_id }},
      "type": "declare",
      "declaration": [
          <value_no_project>,
          <value_project>,
          0,
      ],
    }

   - Replace `<value_no_project>` with our chosen declaration for “No Project.”
   - Replace `<value_project>` with our chosen declaration for “Project.”
   - The third entry is always 0 as specified.


Use these instructions exactly to minimize speculation losses and maximizes share revenue return.
