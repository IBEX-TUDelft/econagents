Instructions for this phase:

1. **Game Information**:
   - Phase: {{ meta.phase_name }} (Phase {{ meta.phase }})
   - Your Role: Owner (Player #{{ meta.player_number }})
   - Name: {{ meta.player_name }}

2. **Available Information**:
  2.1. **Your True Land Values**:
    - {{ private_information.property.name }}:
      - No Project: {{ private_information.property.v[0] }}
      - Project: {{ private_information.property.v[1] }}

  2.2. **Value Boundaries**:
    {% for role, conditions in public_information.boundaries.items() %}
    - {{ role }}:
      {% for condition, bounds in conditions.items() %}
      - {{ condition }}: {{ bounds.low }} to {{ bounds.high }}
      {% endfor %}
    {% endfor %}


  2.3. **Tax Information**:
    - Current tax rate: {{ public_information.tax_rate }}%
The tax (public_information.tax_rate) that is levied for the developer and the owners for the condition that is chosen in the experiment , is equal to 1%.


3. **your declaration decisions to make**

3.1 **Take the real values given in the private information** 
   - `real_value_no_project = {{ private_information.property.v[0] }}`
   - `real_value_project = {{ private_information.property.v[1] }}`

3.2 **Compute declarations** as follows (pseudo-code):
   
   declared_no_project = max(real_value_no_project, real_value_no_project + small_margin)
   declared_project = max(real_value_project, real_value_project + small_margin)


4. **Final Output (JSON)**
   After computing these two declarations, submit them via the required JSON format:

   {
     "gameId": {{ meta.game_id }},
     "type": "declare",
     "declaration": [
       <value_no_project>,
       <value_project>,
       0
     ]
   }
 
   - Replace `<value_no_project>` with our chosen declaration for “No Project.”
   - Replace `<value_project>` with our chosen declaration for “Project.”
   - The third entry is always 0 as specified.


Use these instructions exactly to minimize speculation losses while also keeping the first-declaration tax low.
